import { PersonalizedPipe } from './personalized.pipe';

describe('PersonalizedPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalizedPipe();
    expect(pipe).toBeTruthy();
  });
});
